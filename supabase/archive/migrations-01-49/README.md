# Archived migrations (01–49)

These were applied step-by-step while building the tutoring app. The live database already includes their end state.

**Do not re-run these on the live project.**

Use `../baseline/baseline.sql` (generated from live) for a fresh project. Keep this folder for history / debugging only.

Last archived migration: `49-remove-default-meeting-url.sql`.
